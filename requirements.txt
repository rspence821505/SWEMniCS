# Core dependencies for swe4dvar
# Install with: pip install -r requirements.txt

# Scientific computing
numpy>=2.0
scipy>=1.14

# Visualization
matplotlib>=3.9
seaborn

# I/O and data
h5py
pandas
tqdm

# Logging and utilities
loguru>=0.7.0
checkpoint-schedules>=1.0.0

# Note: FEniCSx ecosystem (dolfinx, basix, ffcx, ufl) and
# parallel computing (mpi4py, petsc4py) should be installed
# via conda from conda-forge channel. See environment.yml.
